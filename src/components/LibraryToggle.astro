---
export interface Props {
  externalCount: number;
  nonExternalCount: number;
  activeFilter?: 'all' | 'community' | 'pwv';
}

const { externalCount, nonExternalCount, activeFilter = 'all' } = Astro.props;
---

<div class="mb-8 flex scroll-mt-20 items-center justify-center" id="view">
  <div class="bg-surface-muted flex rounded-lg p-1">
    <a
      href="/library?page=1"
      class={`px-4 py-2 rounded-md text-sm font-medium transition-all duration-200 hover:text-body hover:bg-surface-hover ${
        activeFilter === 'all'
          ? 'text-body bg-surface shadow-sm'
          : 'text-subtle'
      }`}
    >
      All
      <span class="ml-1 text-xs opacity-75"
        >({externalCount + nonExternalCount})</span
      >
    </a>
    <a
      href="/library?filter=pwv&page=1"
      class={`px-4 py-2 rounded-md text-sm font-medium transition-all duration-200 hover:text-body hover:bg-surface-hover ${
        activeFilter === 'pwv'
          ? 'text-body bg-surface shadow-sm'
          : 'text-subtle'
      }`}
    >
      PWV
      <span class="ml-1 text-xs opacity-75">({nonExternalCount})</span>
    </a>
    <a
      href="/library?filter=community&page=1"
      class={`px-4 py-2 rounded-md text-sm font-medium transition-all duration-200 hover:text-body hover:bg-surface-hover ${
        activeFilter === 'community'
          ? 'text-body bg-surface shadow-sm'
          : 'text-subtle'
      }`}
    >
      Founders &amp; Community
      <span class="ml-1 text-xs opacity-75">({externalCount})</span>
    </a>
  </div>
</div>
